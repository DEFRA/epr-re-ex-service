{
  "packageRules": [
    {
      "matchPackageNames": [
        "defradigital/node",
        "defradigital/node-development"
      ],
      "versioning": "regex:^(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)-node(?<build>\\d+\\.\\d+\\.\\d+)$",
      "groupName": "defra node base images",
      "description": "The tag encodes two versions and only the image version drives the update type, so a Node major jump arrives looking like a minor bump. Hold the Node line at the current LTS (24, active until Oct 2026) - raise this when 26 goes LTS, in step with the mise and engines pins",
      "allowedVersions": "/-node24\\.\\d+\\.\\d+$/"
    }
  ]
}
